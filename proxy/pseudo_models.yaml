# pseudo_models.yaml — Proxy pseudo-model configuration
#
# context_window en pseudo-modelos es intencionalmente menor que el
# context_window de los physical models. Esto obliga al cliente a compactar
# antes de alcanzar el límite real del proveedor, mejorando la experiencia.
# El physical model tiene su propio context_window que es el real.
# NO es un bug — es diseño deliberado.

pseudo_models:
  # ── Razonamiento premium ──────────────────────────────────────────────────
  pensamiento-profundo-caro:
    display_name: Pensamiento Profundo
    description: "Razonamiento de máximo nivel. Límite 120K tokens — si lo superas usa compactalo para continuar."
    input_token_threshold: 120000
    context_window: 120000
    router_llm:
      enabled: true
      suggester: pensamiento-rapido
      suggest_on_downgrade_only: true
    image_handling:
      on_downgrade: auto_describe
    physical_models:
      - provider: opencode-go
        model: anthropic/qwen3.7-max
        api_base: https://opencode.ai/zen/go
        api_key_env: OPENCODE_API_KEY
        openai_tools_compatible: true
        tools_strict: true
        parallel_tools: true
        vision: true
        context_window: 1000000
        note: "Go primary: Qwen3.7 Max vía Anthropic /messages. Premium reasoning (~4.770 req/mes)."
      - provider: deepseek
        model: deepseek/deepseek-v4-pro
        openai_tools_compatible: true
        tools_strict: true
        parallel_tools: true
        vision: true
        context_window: 1000000
        note: "Fallback: DeepSeek V4 Pro directo. Razonamiento profundo."
    fallback_strategy: sequential

  # ── Razonamiento ligero ────────────────────────────────────────────────────
  pensamiento-rapido:
    display_name: Pensamiento Rápido
    description: "Razonamiento ligero. Debug, code review, análisis rápidos. Límite 120K — usa compactalo si lo superas."
    input_token_threshold: 120000
    context_window: 120000
    router_llm:
      enabled: false
    image_handling:
      on_downgrade: auto_describe
    physical_models:
      - provider: opencode-go
        model: openai/qwen3.6-plus
        api_base: https://opencode.ai/zen/go/v1
        api_key_env: OPENCODE_API_KEY
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: true
        vision: true
        context_window: 1000000
        note: "Go primary: Qwen3.6 Plus. Razonamiento ligero (~16.300 req/mes)."
      - provider: deepseek
        model: deepseek/deepseek-v4-flash
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: true
        vision: true
        context_window: 1000000
        note: "Fallback: DeepSeek V4 Flash directo."
    fallback_strategy: sequential

  # ── Desarrollo sostenido ──────────────────────────────────────────────────
  tareas-avanzadas:
    display_name: Tareas Avanzadas
    description: "Desarrollo profundo y sostenido. Límite 200K tokens — usa compactalo si lo superas."
    input_token_threshold: 200000
    context_window: 200000
    router_llm:
      enabled: false
    image_handling:
      on_downgrade: auto_describe
    physical_models:
      - provider: opencode-go
        model: openai/kimi-k2.6
        api_base: https://opencode.ai/zen/go/v1
        api_key_env: OPENCODE_API_KEY
        openai_tools_compatible: true
        tools_strict: true
        parallel_tools: true
        vision: true
        context_window: 128000
        note: "Go primary: Kimi K2.6. Coding avanzado (~5.750 req/mes)."
      - provider: deepseek
        model: deepseek/deepseek-v4-pro
        openai_tools_compatible: true
        tools_strict: true
        parallel_tools: true
        vision: true
        context_window: 1000000
        note: "Fallback 1: DeepSeek V4 Pro directo."
      - provider: deepseek
        model: deepseek/deepseek-v4-flash
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: true
        vision: true
        context_window: 1000000
        note: "Fallback 2: DeepSeek V4 Flash directo."
    fallback_strategy: sequential

  # ── Código preciso (NUEVO) ────────────────────────────────────────────────
  codigo-preciso:
    display_name: Código Preciso
    description: "Generación de código puro. Refactors, tests, parches. Límite 200K — usa compactalo si lo superas."
    input_token_threshold: 200000
    context_window: 200000
    router_llm:
      enabled: false
    image_handling:
      on_downgrade: auto_describe
    physical_models:
      - provider: opencode-go
        model: openai/mimo-v2.5-pro
        api_base: https://opencode.ai/zen/go/v1
        api_key_env: OPENCODE_API_KEY
        openai_tools_compatible: true
        tools_strict: true
        parallel_tools: true
        vision: true
        context_window: 128000
        note: "Go primary: MiMo V2.5 Pro. Especializado en código (~6.450 req/mes)."
      - provider: opencode-go
        model: openai/mimo-v2-pro
        api_base: https://opencode.ai/zen/go/v1
        api_key_env: OPENCODE_API_KEY
        openai_tools_compatible: true
        tools_strict: true
        parallel_tools: true
        vision: true
        context_window: 128000
        note: "Go fallback: MiMo V2 Pro."
      - provider: deepseek
        model: deepseek/deepseek-v4-flash
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: true
        vision: true
        context_window: 1000000
        note: "Fallback: DeepSeek V4 Flash directo."
    fallback_strategy: sequential

  # ── Visión ────────────────────────────────────────────────────────────────
  vision:
    display_name: Visión
    description: "Visión unificada: OCR, diagramas, UI, screenshots. Límite 120K tokens — usa compactalo si superas."
    input_token_threshold: 120000
    context_window: 120000
    router_llm:
      enabled: false
    image_handling:
      on_downgrade: auto_describe
    physical_models:
      - provider: groq
        model: groq/meta-llama/llama-4-scout-17b-16e-instruct
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: false
        vision: true
        context_window: 131072
        note: "Primary: Visión vía Groq. 131K contexto. Rápido y barato."
      - provider: opencode-go
        model: openai/mimo-v2-omni
        api_base: https://opencode.ai/zen/go/v1
        api_key_env: OPENCODE_API_KEY
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: true
        vision: true
        context_window: 128000
        note: "Fallback: MiMo V2 Omni. Visión nativa + texto."
    fallback_strategy: sequential

  # ── Normal (punto de entrada) ─────────────────────────────────────────────
  normal:
    display_name: Normal
    description: "Punto de entrada recomendado. Coding extenso. Límite 500K tokens — usa /compact si lo superas."
    input_token_threshold: 500000
    context_window: 500000
    router_llm:
      enabled: false
    image_handling:
      on_downgrade: auto_describe
    physical_models:
      - provider: opencode-go
        model: openai/kimi-k2.5
        api_base: https://opencode.ai/zen/go/v1
        api_key_env: OPENCODE_API_KEY
        openai_tools_compatible: true
        tools_strict: true
        parallel_tools: true
        vision: true
        context_window: 128000
        note: "Go primary: Kimi K2.5. Rápido, tools estrictas (~9.250 req/mes)."
      - provider: deepseek
        model: deepseek/deepseek-v4-flash
        openai_tools_compatible: true
        tools_strict: true
        parallel_tools: true
        vision: true
        context_window: 1000000
        note: "Fallback: DeepSeek V4 Flash. Rápido, 1M contexto."
    fallback_strategy: sequential

  # ── Gratuito (sin Go) ─────────────────────────────────────────────────────
  normal-gratis:
    display_name: Normal Gratis
    description: "Modelos gratuitos y de bajo costo. Límite 200K tokens — usa /compact si lo superas."
    input_token_threshold: 200000
    context_window: 200000
    router_llm:
      enabled: false
    image_handling:
      on_downgrade: auto_describe
    physical_models:
      - provider: openrouter
        model: openrouter/nvidia/nemotron-3-super-120b-a12b:free
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: false
        vision: true
        note: "Gratuito vía OpenRouter. 1M contexto. Primary."
        context_window: 1000000
      - provider: groq
        model: groq/qwen/qwen3-32b
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: true
        vision: true
        note: "Fallback rápido vía Groq. 32B, 131K contexto. Bajo costo."
        context_window: 131072
    fallback_strategy: sequential

  # ── Masivo rápido ─────────────────────────────────────────────────────────
  massive-fast:
    display_name: Massive Fast
    description: "Texto masivo ultrarrápido. Límite 131K tokens. MiniMax Go + GPT-OSS Groq."
    input_token_threshold: 131072
    context_window: 131072
    router_llm:
      enabled: false
    image_handling:
      on_downgrade: auto_describe
    physical_models:
      - provider: opencode-go
        model: openai/minimax-m2.7
        api_base: https://opencode.ai/zen/go/v1
        api_key_env: OPENCODE_API_KEY
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: true
        vision: true
        context_window: 200000
        note: "Go primary: MiniMax M2.7. Velocidad extrema (~17.000 req/mes)."
      - provider: groq
        model: groq/openai/gpt-oss-20b
        context_window: 131072
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: true
        vision: true
        note: "Fallback: GPT-OSS 20B vía Groq (1000+ t/s)."
    fallback_strategy: sequential

  # ── Flash Lowcost (NUEVO) ──────────────────────────────────────────────────
  flash-lowcost:
    display_name: Flash Lowcost
    description: "Sub-agentes baratos. Clasificación, extracción, parsing. Límite 128K."
    input_token_threshold: 128000
    context_window: 128000
    router_llm:
      enabled: false
    image_handling:
      on_downgrade: auto_describe
    physical_models:
      - provider: opencode-go
        model: openai/qwen3.5-plus
        api_base: https://opencode.ai/zen/go/v1
        api_key_env: OPENCODE_API_KEY
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: true
        vision: true
        context_window: 1000000
        note: "Go primary: Qwen3.5 Plus. El más barato de Go (~50.500 req/mes)."
      - provider: deepseek
        model: deepseek/deepseek-v4-flash
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: true
        vision: true
        context_window: 1000000
        note: "Fallback: DeepSeek V4 Flash directo."
    fallback_strategy: sequential

  # ── Compactador (con helpers internos) ────────────────────────────────────
  compactador:
    display_name: Compactador
    description: "Compactación de contexto con delegación de modelos. GLM-5.1 Go, fallback a GPT-OSS Groq (131K), DeepSeek Flash (1M) para historias masivas."
    input_token_threshold: 3000000
    context_window: 3000000
    router_llm:
      enabled: false
    image_handling:
      on_downgrade: auto_describe
    physical_models:
      - provider: opencode-go
        model: openai/glm-5.1
        api_base: https://opencode.ai/zen/go/v1
        api_key_env: OPENCODE_API_KEY
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: true
        vision: true
        context_window: 128000
        note: "Go primary: GLM-5.1. Compactación vía Go (~4.300 req/mes)."
      - provider: groq
        model: groq/openai/gpt-oss-20b
        context_window: 131072
        openai_tools_compatible: true
        vision: true
        note: "Fallback 1: 20B vía Groq (1000+ t/s). Texto puro para <131K."
      - provider: deepseek
        model: deepseek/deepseek-v4-flash
        context_window: 1000000
        openai_tools_compatible: true
        vision: true
        note: "Fallback 2: 1M contexto. Para historias grandes (>131K tokens)."
      # Modelos helper (no participan en fallback de chat)
      - provider: groq
        model: groq/whisper-large-v3
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: false
        vision: true
        audio: true
        context_window: 131072
        note: "Whisper Large V3 — transcripción de audio (helper interno)."
      - provider: groq
        model: groq/whisper-large-v3-turbo
        openai_tools_compatible: true
        tools_strict: false
        parallel_tools: false
        vision: true
        audio: true
        context_window: 131072
        note: "Whisper Large V3 Turbo — transcripción de audio rápida (helper interno)."
    fallback_strategy: by_context_window

model_aliases:
  gpt-4o: normal
  gpt-4o-mini: normal
  gpt-4.1: tareas-avanzadas
  o3: pensamiento-profundo-caro
  o4-mini: pensamiento-rapido
  gemini-2.5-flash: vision
  gemini-2.5-pro: codigo-preciso
  claude-sonnet-4-20250514: codigo-preciso
  claude-haiku-3-5-20241022: flash-lowcost
  default: normal
